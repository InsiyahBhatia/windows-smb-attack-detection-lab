<Sysmon schemaversion="4.90">
  <!--
    Sysmon Configuration for SOC Home Lab
    Based on: https://github.com/SwiftOnSecurity/sysmon-config
    Purpose: Endpoint telemetry for Blue Team detection lab
  -->

  <HashAlgorithms>md5,sha256,IMPHASH</HashAlgorithms>
  <CheckRevocation/>

  <EventFiltering>

    <!-- ==================== EVENT ID 1 - Process Creation ==================== -->
    <!-- Captures all process creation with full command line -->
    <RuleGroup name="" groupRelation="or">
      <ProcessCreate onmatch="exclude">
        <!-- Exclude high-noise legitimate processes -->
        <Image condition="is">C:\Windows\System32\backgroundTaskHost.exe</Image>
        <Image condition="is">C:\Windows\System32\WerFault.exe</Image>
      </ProcessCreate>
    </RuleGroup>

    <!-- ==================== EVENT ID 2 - File Creation Time Changed ========= -->
    <RuleGroup name="" groupRelation="or">
      <FileCreateTime onmatch="include">
        <TargetFilename condition="contains">Downloads</TargetFilename>
        <TargetFilename condition="contains">Temp</TargetFilename>
        <TargetFilename condition="end with">.exe</TargetFilename>
        <TargetFilename condition="end with">.ps1</TargetFilename>
        <TargetFilename condition="end with">.bat</TargetFilename>
      </FileCreateTime>
    </RuleGroup>

    <!-- ==================== EVENT ID 3 - Network Connection ================= -->
    <!-- Captures all outbound and inbound network connections -->
    <RuleGroup name="" groupRelation="or">
      <NetworkConnect onmatch="exclude">
        <!-- Exclude high-volume trusted connections -->
        <Image condition="is">C:\Program Files\Windows Defender\MsMpEng.exe</Image>
        <DestinationPort condition="is">53</DestinationPort>
      </NetworkConnect>
    </RuleGroup>

    <!-- ==================== EVENT ID 5 - Process Terminated ================= -->
    <RuleGroup name="" groupRelation="or">
      <ProcessTerminate onmatch="include">
        <Image condition="contains">powershell</Image>
        <Image condition="contains">cmd.exe</Image>
        <Image condition="contains">wscript</Image>
        <Image condition="contains">cscript</Image>
        <Image condition="contains">mshta</Image>
      </ProcessTerminate>
    </RuleGroup>

    <!-- ==================== EVENT ID 7 - Image Loaded ======================= -->
    <RuleGroup name="" groupRelation="or">
      <ImageLoad onmatch="include">
        <!-- Monitor for credential-access DLL loads -->
        <ImageLoaded condition="contains">samlib.dll</ImageLoaded>
        <ImageLoaded condition="contains">vaultcli.dll</ImageLoaded>
        <ImageLoaded condition="contains">wdigest.dll</ImageLoaded>
      </ImageLoad>
    </RuleGroup>

    <!-- ==================== EVENT ID 8 - CreateRemoteThread ================= -->
    <RuleGroup name="" groupRelation="or">
      <CreateRemoteThread onmatch="include">
        <!-- All remote thread creation is suspicious - include everything -->
      </CreateRemoteThread>
    </RuleGroup>

    <!-- ==================== EVENT ID 10 - Process Access ==================== -->
    <RuleGroup name="" groupRelation="or">
      <ProcessAccess onmatch="include">
        <!-- Credential dumping targets LSASS -->
        <TargetImage condition="is">C:\Windows\system32\lsass.exe</TargetImage>
        <!-- Sysmon self-protection -->
        <TargetImage condition="is">C:\Windows\Sysmon64.exe</TargetImage>
      </ProcessAccess>
    </RuleGroup>

    <!-- ==================== EVENT ID 11 - File Created ====================== -->
    <RuleGroup name="" groupRelation="or">
      <FileCreate onmatch="include">
        <TargetFilename condition="end with">.exe</TargetFilename>
        <TargetFilename condition="end with">.ps1</TargetFilename>
        <TargetFilename condition="end with">.bat</TargetFilename>
        <TargetFilename condition="end with">.vbs</TargetFilename>
        <TargetFilename condition="contains">Temp</TargetFilename>
        <TargetFilename condition="contains">AppData\Roaming</TargetFilename>
      </FileCreate>
    </RuleGroup>

    <!-- ==================== EVENT ID 13 - Registry Value Set ================ -->
    <RuleGroup name="" groupRelation="or">
      <RegistryEvent onmatch="include">
        <!-- Persistence via Run keys -->
        <TargetObject condition="contains">CurrentVersion\Run</TargetObject>
        <TargetObject condition="contains">CurrentVersion\RunOnce</TargetObject>
        <!-- Disable Windows Defender -->
        <TargetObject condition="contains">DisableAntiSpyware</TargetObject>
        <!-- UAC bypass -->
        <TargetObject condition="contains">EnableLUA</TargetObject>
      </RegistryEvent>
    </RuleGroup>

    <!-- ==================== EVENT ID 22 - DNS Query ========================= -->
    <RuleGroup name="" groupRelation="or">
      <DnsQuery onmatch="exclude">
        <!-- Exclude common Microsoft domains -->
        <QueryName condition="end with">.microsoft.com</QueryName>
        <QueryName condition="end with">.windows.com</QueryName>
        <QueryName condition="end with">.windowsupdate.com</QueryName>
      </DnsQuery>
    </RuleGroup>

  </EventFiltering>

</Sysmon>
